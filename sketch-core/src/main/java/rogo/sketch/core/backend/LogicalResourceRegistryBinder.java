package rogo.sketch.core.backend;

import rogo.sketch.core.resource.GraphicsResourceManager;

public interface LogicalResourceRegistryBinder {
    void bindLogicalResourceRegistry(GraphicsResourceManager resourceManager);
}
